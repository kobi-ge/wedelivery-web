apiVersion: v1
kind: Service
metadata:
  name: db
  labels:
    app: wedone
    tier: database
spec:
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: wedone
    tier: database
  clusterIP: None  # Headless service for StatefulSet identity
