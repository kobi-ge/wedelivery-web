apiVersion: v1
kind: Service
metadata:
  name: wedone-api
  labels:
    app: wedone
    tier: backend
spec:
  type: ClusterIP # Changed from LoadBalancer due to quota limits
  ports:
    - port: 80
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    app: wedone
    tier: backend
